services:
  fichedepute:
    build: .
    container_name: fichedepute
    restart: unless-stopped
    network_mode: host
    environment:
      - PORT=10091
      - VAR_DIR=/app/var
    volumes:
      - ./var:/app/var
